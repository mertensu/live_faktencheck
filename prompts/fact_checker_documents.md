<primary_sources>
The following documents are available via the `search_document` tool:
{document_list}

When citing information from these documents, always include the source and page number.
**Format:** [Document_Name, p. X] — if no page number is returned, cite only the document name.
</primary_sources>
